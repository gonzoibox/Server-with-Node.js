doctype html
html
    head
        title Page title
        link(rel='stylesheet', href='/styles.css')
    body
        if username
            h1 Hello, #{username}!
        else
            h1 Enter
            form(method='POST')
                input(type='text' name='username')
                button(type='submit') Enter
        